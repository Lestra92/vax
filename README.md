# Create VAX image

#### navigate to vax folder

    cd vax

#### run build command

    docker build --tag vax .

#### when build finishes, navigate back

    cd ..

#### start docker compose

    docker-compose up -d

#### test with postman    